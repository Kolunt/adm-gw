# Конфигурация проекта для локальной и production среды

## 🎯 Автоматическое определение окружения

Проект автоматически определяет, запущен ли он на PythonAnywhere или локально, и применяет соответствующие настройки.

### Определение окружения

Система проверяет следующие условия для определения production среды:
- Наличие переменной окружения `PYTHONANYWHERE_DOMAIN`
- Наличие `pythonanywhere.com` в `HTTP_HOST` или `SERVER_NAME`

### Production домен

Проект настроен на работу с доменом:
**https://gwadm.pythonanywhere.com**

## 📋 Конфигурация

### Backend (`backend/main.py`)

#### База данных
- **Локально**: `sqlite:///./santa.db` (в папке `backend/`)
- **Production**: `sqlite:////home/ваш_username/gwadm/backend/santa.db`

#### Пути для загрузки файлов
- **Локально**: `backend/uploads/`
- **Production**: `/home/ваш_username/gwadm/backend/uploads/`

#### CORS Origins
Автоматически настроены:
- **Production**: `https://gwadm.pythonanywhere.com`, `http://localhost:3000`, `http://127.0.0.1:3000`
- **Локально**: `http://localhost:3000`, `http://127.0.0.1:3000`, `https://gwadm.pythonanywhere.com`

Можно переопределить через переменную окружения:
```bash
export CORS_ORIGINS="https://yourdomain.com,http://localhost:3000"
```

### Frontend (`src/utils/axiosConfig.js`)

#### API URL
- **Production**: используется относительный путь (фронтенд и backend на одном домене)
- **Локально**: `http://localhost:8006`

Автоматическое определение:
```javascript
axios.defaults.baseURL = process.env.NODE_ENV === 'production' ? '' : 'http://localhost:8006';
```

## 🔧 Переменные окружения

### Backend

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `CORS_ORIGINS` | Разрешенные домены для CORS | Автоматически определяется |
| `PYTHONANYWHERE_DOMAIN` | Устанавливается автоматически на PythonAnywhere | - |

### Frontend

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `REACT_APP_CROSS_SERVER_REDIRECT_URL` | URL для кросс-серверного логина | `https://gwadm.pythonanywhere.com` |
| `NODE_ENV` | Окружение (development/production) | Автоматически определяется при сборке |

## 🚀 Использование

### Локальная разработка

1. Backend запускается на `http://localhost:8006`
2. Frontend запускается на `http://localhost:3000`
3. Frontend автоматически обращается к backend на порту 8006

### Production (PythonAnywhere)

1. Backend и Frontend работают на `https://gwadm.pythonanywhere.com`
2. Frontend использует относительные пути для API запросов
3. Все настройки применяются автоматически

## ✅ Проверка конфигурации

### Проверка на локальной машине

```bash
# Проверить переменные окружения
python -c "import os; print('IS_PYTHONANYWHERE:', 'pythonanywhere.com' in os.environ.get('HTTP_HOST', ''))"

# Запустить backend
cd backend
python main.py

# В другом терминале запустить frontend
npm start
```

### Проверка на PythonAnywhere

```bash
# На PythonAnywhere проверьте окружение
python3.10 -c "import os; print('IS_PYTHONANYWHERE:', 'PYTHONANYWHERE_DOMAIN' in os.environ)"

# Проверьте логи в Web → Error log
```

## 📝 Файлы конфигурации

- `backend/main.py` - основной конфигурационный файл с автоматическим определением окружения
- `backend/config.py` - дополнительный файл конфигурации (опционально)
- `backend/pythonanywhere_config.py` - специфичные настройки для PythonAnywhere
- `src/utils/axiosConfig.js` - конфигурация API клиента для фронтенда

## 🔄 Обновление конфигурации

При изменении домена обновите следующие файлы:
1. `backend/main.py` - CORS origins
2. `backend/pythonanywhere_config.py` - `PRODUCTION_DOMAIN`
3. `src/pages/Auth/CrossServerLogin.js` - `REACT_APP_CROSS_SERVER_REDIRECT_URL`
4. `src/utils/axiosConfig.js` - если нужно изменить логику определения API URL

