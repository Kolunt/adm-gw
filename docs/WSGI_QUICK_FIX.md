# Быстрое исправление WSGI: ModuleNotFoundError

## 🔴 Проблема

```
ModuleNotFoundError: No module named 'main'
File "/var/www/gwadm_pythonanywhere_com_wsgi.py", line 86
```

## ✅ Решение (2 минуты)

### Шаг 1: Откройте WSGI конфигурацию

1. PythonAnywhere → **Web**
2. Нажмите на **WSGI configuration file** (обычно `/var/www/gwadm_pythonanywhere_com_wsgi.py`)

### Шаг 2: Замените содержимое

**Скопируйте весь код из файла `backend/wsgi_pythonanywhere.py`** и вставьте в WSGI конфигурацию.

Этот файл:
- ✅ Автоматически определит username
- ✅ Проверит все возможные пути к проекту
- ✅ Найдет правильный путь к `main.py`
- ✅ Выведет подробную диагностику в Error log

### Шаг 3: Сохраните и перезапустите

1. Нажмите **Save**
2. Нажмите **Reload** в разделе Web
3. Проверьте **Error log** — там будет подробная диагностика

### Шаг 4: Проверьте результат

В Error log должно быть:
```
✅ Using project path: /home/gwadm/gwadm/backend
✅ main.py found at: /home/gwadm/gwadm/backend/main.py
✅ Successfully imported app
✅ WSGI application configured successfully
```

Если все ✅ — проблема решена!

## 🔍 Если проблема сохраняется

### Проверьте структуру проекта

В Bash консоли:
```bash
cd ~
find . -name "main.py" -type f
```

Это покажет, где на самом деле находится `main.py`.

### Обновите проект из Git

```bash
cd ~/gwadm
git pull origin master
ls -la backend/main.py  # Должен существовать
```

### Если main.py в другом месте

Используйте вывод `find . -name "main.py"` и обновите путь в WSGI файле соответственно.

## 📚 Дополнительная документация

- `docs/WSGI_CONFIGURATION.md` — полная инструкция по WSGI
- `docs/MAIN_PY_NOT_FOUND.md` — что делать если main.py не найден
- `docs/TROUBLESHOOTING.md` — общая диагностика

