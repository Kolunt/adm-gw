# Развертывание фронтенда на Production (PythonAnywhere) через Git

## ✅ Важно: Build включен в репозиторий

Папка `build/` уже находится в Git репозитории, поэтому она автоматически скачивается при `git clone` или `git pull`.

## 🎯 Обновление build через Git

Это самый простой и надежный способ обновления фронтенда.

### Шаг 1: Соберите фронтенд локально

На вашем компьютере:

```bash
cd C:\Users\TBG\Documents\adm-gw  # или ваш путь к проекту
npm install
npm run build
```

Это создаст папку `build/` в корне проекта со следующей структурой:
```
build/
├── index.html
├── static/
│   ├── css/
│   ├── js/
│   └── media/
├── favicon.ico
└── manifest.json
```

### Шаг 2: Закоммитьте и запушьте build

```bash
git add build/
git commit -m "Update production frontend build"
git push origin master
```

**Важно:** Убедитесь, что `build/` не находится в `.gitignore`. Если он там есть, временно удалите его из `.gitignore` или используйте `git add -f build/`.

### Шаг 3: Обновите на PythonAnywhere

В Bash консоли PythonAnywhere:

```bash
cd ~/gwadm
git pull origin master
```

### Шаг 4: Проверьте структуру файлов

```bash
cd ~/gwadm
ls -la build/              # Проверка наличия build папки
ls -la build/index.html    # Проверка index.html
ls -la build/static/       # Проверка статических файлов
```

Убедитесь, что структура правильная:
```
~/gwadm/
├── backend/
│   ├── main.py
│   └── ...
└── build/          # ← Должна быть здесь!
    ├── index.html
    ├── static/
    └── ...
```

### Шаг 5: Перезапустите веб-приложение

1. Откройте PythonAnywhere → **Web**
2. Нажмите зеленую кнопку **Reload**
3. Проверьте **Error log** на наличие ошибок

### Шаг 6: Проверьте работу

1. Откройте `https://gwadm.pythonanywhere.com`
   - Должна загрузиться главная страница React приложения

2. Откройте `https://gwadm.pythonanywhere.com/my_login_page`
   - Должна загрузиться страница кросс-серверного логина (не 404!)

3. Проверьте другие роуты:
   - `https://gwadm.pythonanywhere.com/login`
   - `https://gwadm.pythonanywhere.com/cross-server-login`
   - Все должны работать без 404

## 🔄 Обновление фронтенда

Когда вы вносите изменения во фронтенд:

1. **Локально:**
   ```bash
   npm run build
   git add build/
   git commit -m "Update frontend: описание изменений"
   git push origin master
   ```

2. **На PythonAnywhere:**
   ```bash
   cd ~/gwadm
   git pull origin master
   ```

3. **Перезапустите приложение:** Web → Reload

## 🚨 Альтернативный способ: Ручная загрузка (если Git не используется)

Если по какой-то причине вы не используете Git для развертывания:

1. Соберите фронтенд локально: `npm run build`
2. Откройте PythonAnywhere → **Files**
3. Перейдите в `~/gwadm/`
4. Загрузите папку `build/` с вашего компьютера
5. Перезапустите приложение: Web → Reload

## 🛠️ Устранение неполадок

### Проблема: Все еще 404

**Решение:**
1. Проверьте, что папка `build` существует: `ls -la ~/gwadm/build`
2. Проверьте, что `index.html` существует: `ls -la ~/gwadm/build/index.html`
3. Проверьте логи ошибок в PythonAnywhere → Web → Error log
4. Убедитесь, что приложение перезапущено (Reload)
5. Проверьте, что структура правильная: `cd ~/gwadm && tree build -L 2`

### Проблема: "Frontend not found"

**Решение:**
Это означает, что backend не может найти папку `build`. Проверьте:
- Папка должна быть в `~/gwadm/build/` (не в `~/gwadm/backend/build/`)
- Проверьте права доступа: `chmod -R 755 ~/gwadm/build`
- Backend автоматически проверяет следующие пути:
  - `../build` (относительно backend/)
  - `build` (в backend/)
  - `dist` (старый путь)
  - `../dist`

### Проблема: Статические файлы не загружаются

**Решение:**
1. Проверьте, что папка `static/` существует: `ls -la ~/gwadm/build/static`
2. Проверьте права доступа: `chmod -R 755 ~/gwadm/build/static`

### Проблема: Build не коммитится в Git

**Решение:**
Если `build/` находится в `.gitignore`, вы можете:
1. Временно удалить `build/` из `.gitignore`
2. Или использовать принудительное добавление: `git add -f build/`
3. Или использовать опцию сборки на PythonAnywhere (см. альтернативный способ выше)

## 📦 Автоматизация через скрипт

Можно создать скрипт для автоматического развертывания:

```bash
#!/bin/bash
# deploy_frontend.sh

# Сборка локально
npm run build

# Коммит и пуш
git add build/
git commit -m "Auto-update frontend build: $(date)"
git push origin master

echo "✅ Frontend обновлен! Теперь выполните на PythonAnywhere:"
echo "   cd ~/gwadm && git pull origin master"
```

## 📝 Примечания

- **Размер репозитория:** Папка `build/` может быть большой (обычно 1-5 MB). Если это проблема, рассмотрите использование Git LFS или сборку на PythonAnywhere.
- **Частота обновлений:** Обновляйте build только когда вносятся изменения во фронтенд.
- **Версионирование:** В сообщениях коммитов указывайте версию или описание изменений для удобства отслеживания.
