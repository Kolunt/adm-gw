# Устранение ошибки 500 на PythonAnywhere

## 🔴 Симптомы

В логах появляются ошибки:
```
GET / HTTP/1.1" 500
```

## ✅ Диагностика

### Шаг 1: Проверьте Error log на PythonAnywhere

1. Откройте **Web** → **Error log**
2. Найдите последние ошибки
3. Скопируйте полный текст ошибки

### Шаг 2: Проверьте Server log

1. Откройте **Web** → **Server log**
2. Найдите сообщения о запуске приложения
3. Проверьте, найдена ли папка `build`:
   - Должно быть: `✅ Frontend directory found: /home/username/gwadm/build`
   - Или: `⚠️ Frontend directory not found`

### Шаг 3: Проверьте наличие фронтенда

В Bash консоли PythonAnywhere выполните:

```bash
cd ~/gwadm
ls -la build/  # Должна существовать папка build
ls -la build/index.html  # Должен существовать index.html
```

Если папка `build/` отсутствует:

```bash
cd ~/gwadm
npm install
npm run build
git add build/
git commit -m "Add frontend build"
git push origin master
```

### Шаг 4: Проверьте WSGI конфигурацию

1. Откройте **Web** → **WSGI configuration file**
2. Убедитесь, что используется вариант с автоматическим определением username:

```python
import sys
import os

# Автоматическое определение username
username = os.environ.get('USER', os.path.expanduser('~').split('/')[-1])
home_dir = os.path.expanduser('~')

project_path = os.path.join(home_dir, 'gwadm', 'backend')

if project_path not in sys.path:
    sys.path.insert(0, project_path)

os.chdir(project_path)

from main import app
application = app
```

3. Нажмите **Save**
4. Нажмите **Reload** в разделе Web

## 🔍 Частые причины ошибки 500

### 1. Фронтенд не собран

**Симптом:** В Server log: `⚠️ Frontend directory not found`

**Решение:**
```bash
cd ~/gwadm
npm run build
git add build/
git commit -m "Add frontend build"
git push origin master
```

Затем перезапустите приложение (Reload в разделе Web).

### 2. Неправильный путь к фронтенду

**Симптом:** В Server log показаны неправильные пути

**Решение:**
Проверьте структуру проекта:
```bash
cd ~/gwadm
pwd  # Должно быть /home/username/gwadm
ls -la
# Должны быть: backend/, build/, package.json, src/
```

Фронтенд должен быть в `~/gwadm/build/`, а не в `~/gwadm/backend/build/`.

### 3. Ошибка в WSGI конфигурации

**Симптом:** `ModuleNotFoundError: No module named 'main'`

**Решение:**
Используйте WSGI конфигурацию с автоматическим определением username (см. Шаг 4 выше).

### 4. База данных не инициализирована

**Симптом:** Ошибки связанные с таблицами в БД

**Решение:**
База данных должна инициализироваться автоматически при первом запуске. Если нет:

```bash
cd ~/gwadm/backend
python3.10 -c "from main import *; print('Database initialized')"
```

### 5. Ошибка импорта модулей

**Симптом:** `ImportError` или `ModuleNotFoundError` для других модулей

**Решение:**
Установите зависимости:
```bash
cd ~/gwadm/backend
pip3.10 install --user -r requirements.txt
```

## 📝 Проверка после исправления

После применения исправлений:

1. Нажмите **Reload** в разделе Web
2. Откройте сайт: `https://gwadm.pythonanywhere.com/`
3. Проверьте Error log — ошибок быть не должно
4. Проверьте Server log — должно быть: `✅ Frontend directory found`

## 🆘 Если проблема сохраняется

1. Скопируйте полный текст из **Error log**
2. Скопируйте последние строки из **Server log** (около 50 строк)
3. Проверьте структуру проекта:
   ```bash
   cd ~/gwadm
   tree -L 2 -I 'node_modules'  # или ls -R
   ```

## 📚 Дополнительная документация

- `docs/WSGI_CONFIGURATION.md` — конфигурация WSGI
- `docs/PYTHONANYWHERE_QUICK_START.md` — быстрый старт
- `docs/PRODUCTION_FRONTEND_DEPLOY.md` — развертывание фронтенда

