# Деплой на PythonAnywhere.com

## 📋 Что такое PythonAnywhere?

PythonAnywhere — это облачная платформа для хостинга Python веб-приложений. Поддерживает:
- ✅ FastAPI, Django, Flask
- ✅ SQLite, MySQL, PostgreSQL
- ✅ Статические файлы
- ✅ Scheduled tasks
- ✅ Webhooks

## 🎯 Что нужно для деплоя

### 1. Аккаунт PythonAnywhere

**Бесплатный план (Beginner):**
- ✅ До 2 веб-приложений
- ✅ SQLite база данных
- ✅ 512 MB дискового пространства
- ✅ Доступ к Bash консоли
- ⚠️ Приложение должно быть публично доступным (или использовать ваш домен)

**Платные платы:**
- Больше ресурсов
- MySQL/PostgreSQL
- Приватные приложения

### 2. Структура проекта

Ваш проект уже готов:
```
backend/
  ├── main.py          # FastAPI приложение
  ├── requirements.txt # Зависимости
  ├── santa.db        # SQLite база данных (будет создана автоматически)
  ├── uploads/        # Загруженные файлы
  └── telegram_bot.py # Telegram бот (опционально)
```

## 📝 Пошаговая инструкция

### Шаг 1: Регистрация на PythonAnywhere

1. Перейдите на https://www.pythonanywhere.com
2. Зарегистрируйтесь (можно через GitHub)
3. Войдите в аккаунт

### Шаг 2: Загрузка кода

#### Вариант A: Через Git (рекомендуется)

1. В консоли PythonAnywhere:
```bash
cd ~
git clone https://github.com/Kolunt/gwadm.git
cd gwadm
```

#### Вариант B: Через загрузку файлов

1. В панели управления PythonAnywhere откройте **Files**
2. Перейдите в `home/ваш_username/`
3. Загрузите все файлы из папки `backend/`

### Шаг 3: Установка зависимостей

В консоли PythonAnywhere:

```bash
cd ~/gwadm/backend  # или куда вы загрузили файлы
pip3.10 install --user -r requirements.txt
```

**Важно:** 
- Если вы клонировали репозиторий через Git, файл `requirements.txt` уже находится в папке `backend/`
- Если вы загрузили файлы вручную, убедитесь, что `requirements.txt` находится в той же папке, где `main.py`

**Примечание:** PythonAnywhere использует Python 3.10 по умолчанию. Используйте `pip3.10` или `pip3.9` в зависимости от вашей версии.

### Шаг 4: Настройка базы данных

База данных создастся автоматически при первом запуске, но можно создать вручную:

```bash
cd ~/gwadm/backend
python3.10 main.py
# Нажмите Ctrl+C после создания БД
```

Или через Python консоль:
```bash
python3.10
>>> from main import Base, engine
>>> Base.metadata.create_all(bind=engine)
>>> exit()
```

### Шаг 5: Настройка веб-приложения

1. В панели управления откройте **Web**
2. Нажмите **Add a new web app**
3. Выберите:
   - **Manual configuration**
   - **Python 3.10** (или ваша версия)

### Шаг 6: Конфигурация WSGI файла

1. В разделе **Web** найдите **WSGI configuration file**
2. Нажмите на ссылку (обычно `/var/www/yourusername_pythonanywhere_com_wsgi.py`)
3. Замените содержимое на:

```python
import sys
import os

# Добавляем путь к проекту
path = '/home/yourusername/gwadm/backend'
if path not in sys.path:
    sys.path.insert(0, path)

# Меняем рабочую директорию
os.chdir(path)

# Импортируем приложение
from main import app

# Переменная application обязательна для PythonAnywhere
application = app
```

**Важно:** Замените `yourusername` на ваш реальный username на PythonAnywhere!

### Шаг 7: Настройка статических файлов

В разделе **Web** → **Static files** добавьте:

- **URL:** `/static/`
- **Directory:** `/home/yourusername/gwadm/backend/static` (если есть)

Для загруженных файлов (uploads):

- **URL:** `/uploads/`
- **Directory:** `/home/yourusername/gwadm/backend/uploads`

### Шаг 8: Обновление CORS настроек

Откройте `backend/main.py` и обновите CORS:

```python
# Найдите строку около 800:
app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000", "http://127.0.0.1:3000"],
    ...
)

# Замените на:
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",
        "http://127.0.0.1:3000",
        "https://kolunt.github.io",  # GitHub Pages
        "https://yourusername.pythonanywhere.com",  # Ваш PythonAnywhere домен
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Шаг 9: Настройка переменных окружения (если нужно)

В PythonAnywhere можно использовать переменные окружения:

1. Откройте **Web** → **Environment variables**
2. Добавьте нужные переменные (например, `SECRET_KEY`, `DATABASE_URL`)

### Шаг 10: Создание директорий

Убедитесь, что директория для uploads существует:

```bash
mkdir -p ~/gwadm/backend/uploads/icons
```

### Шаг 11: Обновление путей в коде (опционально)

Если нужно изменить пути к базе данных:

В `backend/main.py` найдите:
```python
SQLALCHEMY_DATABASE_URL = "sqlite:///./santa.db"
```

Для абсолютного пути:
```python
import os
DB_PATH = os.path.join(os.path.dirname(__file__), "santa.db")
SQLALCHEMY_DATABASE_URL = f"sqlite:///{DB_PATH}"
```

### Шаг 12: Перезапуск приложения

1. В разделе **Web** нажмите зеленую кнопку **Reload**
2. Проверьте логи в разделе **Error log** и **Server log**

## 🔧 Дополнительные настройки

### Настройка домена (для бесплатного плана)

Ваш сайт будет доступен по адресу:
```
https://yourusername.pythonanywhere.com
```

### Настройка кастомного домена (для платных планов)

1. В разделе **Web** найдите **Add a new domain**
2. Добавьте свой домен
3. Настройте DNS записи согласно инструкциям PythonAnywhere

### Scheduled Tasks (для Telegram бота)

Если нужно запускать задачи по расписанию:

1. Откройте **Tasks**
2. Добавьте новую задачу:
   - **Command:** `python3.10 /home/yourusername/gwadm/backend/telegram_bot.py`
   - **Schedule:** ежедневно, еженедельно и т.д.

### MySQL/PostgreSQL (для платных планов)

Если нужно использовать MySQL или PostgreSQL:

1. Создайте базу данных в разделе **Databases**
2. Обновите `SQLALCHEMY_DATABASE_URL`:
   ```python
   # MySQL
   SQLALCHEMY_DATABASE_URL = "mysql+pymysql://username:password@hostname/dbname"
   
   # PostgreSQL
   SQLALCHEMY_DATABASE_URL = "postgresql://username:password@hostname/dbname"
   ```

## ✅ Проверка работоспособности

1. Откройте ваш сайт: `https://yourusername.pythonanywhere.com`
2. Проверьте `/docs` — должна открыться Swagger документация FastAPI
3. Проверьте API endpoints

## 🔄 Обновление приложения

### Через Git:

```bash
cd ~/gwadm
git pull origin master
```

Затем нажмите **Reload** в разделе **Web**.

### Через загрузку файлов:

1. Загрузите новые файлы через **Files**
2. Нажмите **Reload** в разделе **Web**

## 🐛 Решение проблем

### Проблема: "Module not found"

**Решение:**
```bash
pip3.10 install --user название_модуля
```

### Проблема: "Permission denied"

**Решение:**
Проверьте права доступа к файлам:
```bash
chmod 755 ~/gwadm/backend
chmod 644 ~/gwadm/backend/*.py
```

### Проблема: "Database locked"

**Решение:**
SQLite на PythonAnywhere может иметь проблемы с параллельным доступом. Рассмотрите переход на MySQL/PostgreSQL.

### Проблема: CORS ошибки

**Решение:**
Убедитесь, что все домены добавлены в `allow_origins` в `main.py`.

### Проблема: Файлы не загружаются

**Решение:**
1. Проверьте, что директория `uploads` существует
2. Проверьте права доступа: `chmod 777 ~/gwadm/backend/uploads`
3. Проверьте настройки Static files в Web

### Проблема: Приложение не запускается

**Решение:**
1. Проверьте **Error log** в разделе Web
2. Проверьте синтаксис Python кода
3. Убедитесь, что все зависимости установлены

## 📊 Лимиты бесплатного плана

- ⚠️ Приложение должно быть публично доступным
- ⚠️ Ограничение на CPU время (использование должно быть разумным)
- ⚠️ Лимит на внешние HTTP запросы
- ⚠️ SQLite только (для MySQL/PostgreSQL нужен платный план)

## 🔗 Интеграция с GitHub Pages Frontend

После деплоя backend на PythonAnywhere:

1. Обновите `src/utils/axiosConfig.js`:
```javascript
axios.defaults.baseURL = process.env.NODE_ENV === 'production' 
  ? 'https://yourusername.pythonanywhere.com' 
  : 'http://localhost:8006';
```

2. Добавьте переменную окружения для production:
```javascript
// Используйте переменную окружения или проверку домена
const getApiUrl = () => {
  if (window.location.hostname === 'kolunt.github.io') {
    return 'https://yourusername.pythonanywhere.com';
  }
  return 'http://localhost:8006';
};

axios.defaults.baseURL = getApiUrl();
```

3. Пересоберите и задеплойте frontend на GitHub Pages

## ✅ Чеклист деплоя

- [ ] Зарегистрирован аккаунт на PythonAnywhere
- [ ] Загружен код (через Git или файлы)
- [ ] Установлены зависимости (`pip3.10 install --user -r requirements.txt`)
- [ ] Создана/настроена база данных
- [ ] Создан Web app (Manual configuration)
- [ ] Настроен WSGI файл
- [ ] Настроены Static files
- [ ] Обновлены CORS настройки
- [ ] Созданы необходимые директории
- [ ] Приложение перезапущено (Reload)
- [ ] Проверена работоспособность (`/docs`)
- [ ] Обновлен frontend для использования production API

## 🎉 Готово!

После выполнения всех шагов ваш backend будет работать на:
**https://yourusername.pythonanywhere.com**

Frontend на GitHub Pages будет обращаться к этому backend автоматически.

