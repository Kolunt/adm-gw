# Диагностика белого экрана на PythonAnywhere

## 🔴 Симптомы

- Сайт открывается, но показывает белый экран
- Страница в бесконечной загрузке
- Error log пустой (нет ошибок)
- Server log показывает успешный запуск

## ✅ Диагностика

### Шаг 1: Проверьте Server log при запросе

1. Откройте сайт в браузере: `https://gwadm.pythonanywhere.com/`
2. Сразу после этого откройте **Web** → **Server log**
3. Найдите строки с `WSGI REQUEST:` и `WSGI RESPONSE:`

Должно быть что-то вроде:
```
WSGI REQUEST: GET /
WSGI RESPONSE: 200 15 headers
WSGI RESPONSE BODY: 12345 bytes
```

### Шаг 2: Проверьте в браузере (F12)

1. Откройте Developer Tools (F12)
2. Перейдите на вкладку **Network**
3. Обновите страницу (Ctrl+R или F5)
4. Проверьте:
   - Загружается ли `index.html`?
   - Загружаются ли файлы из `/static/`?
   - Есть ли ошибки в консоли (Console tab)?

### Шаг 3: Частые причины

#### 1. JavaScript ошибки в консоли

**Проверка:**
- Откройте Console в браузере (F12 → Console)
- Посмотрите, есть ли красные ошибки

**Типичные ошибки:**
- `Failed to fetch` - проблема с API подключением
- `Cannot read property...` - ошибка в JavaScript коде
- CORS ошибки - проблема с настройками CORS

#### 2. Статические файлы не загружаются

**Проверка:**
```bash
cd ~/gwadm
ls -la build/static/  # Должна существовать папка static
ls -la build/index.html  # Должен существовать index.html
```

**Если файлы отсутствуют:**
```bash
cd ~/gwadm
npm run build
git add build/
git commit -m "Rebuild frontend"
git push origin master
```

#### 3. Неправильный путь к API

**Проверка:**
В Network tab проверьте запросы к API. Они должны идти на тот же домен.

**Решение:**
Проверьте настройки API в `src/utils/axiosConfig.js` или аналогичном файле.

#### 4. Проблемы с CORS

**Проверка:**
В Console должна быть ошибка вида:
```
Access to fetch at '...' from origin '...' has been blocked by CORS policy
```

**Решение:**
Проверьте настройки CORS в `backend/main.py`.

### Шаг 4: Проверка структуры ответа

Проверьте, что корневой путь "/" возвращает правильный HTML:

```bash
curl https://gwadm.pythonanywhere.com/
```

Должен вернуться HTML из `index.html`.

### Шаг 5: Проверка статических файлов

```bash
curl https://gwadm.pythonanywhere.com/static/js/main.xxxxx.js
curl https://gwadm.pythonanywhere.com/static/css/main.xxxxx.css
```

Эти файлы должны загружаться.

## 🔍 Детальная диагностика

### Проверка в Network tab

1. Откройте F12 → Network
2. Обновите страницу
3. Проверьте статус каждого запроса:
   - ✅ Зеленые (200, 304) - OK
   - ❌ Красные (404, 500) - Проблема
   - ⏳ Висящие запросы - Проблема с ответом

### Проверка консоли браузера

1. Откройте F12 → Console
2. Посмотрите на ошибки:
   - **Красные ошибки** - это основная проблема
   - **Желтые предупреждения** - обычно не критично

### Проверка HTML ответа

1. Откройте сайт
2. Правый клик → View Page Source
3. Проверьте:
   - Есть ли содержимое HTML?
   - Правильно ли указаны пути к статическим файлам?
   - Есть ли `<script>` и `<link>` теги?

## 🆘 Если проблема сохраняется

1. Скопируйте вывод из **Console** (F12 → Console)
2. Скопируйте вывод из **Network** tab (какие запросы висят)
3. Скопируйте последние строки из **Server log** (после запроса к сайту)

Это поможет точно определить проблему.

## 📝 Типичные решения

### Решение 1: Пересобрать фронтенд

```bash
cd ~/gwadm
npm run build
git add build/
git commit -m "Rebuild frontend"
git push origin master
```

### Решение 2: Проверить пути к статическим файлам

В `package.json` проверьте `homepage` (если есть) - должен быть пустым или указывать на правильный домен.

### Решение 3: Проверить настройки API

В `src/utils/axiosConfig.js` проверьте базовый URL API - должен быть относительным или указывать на правильный домен.

