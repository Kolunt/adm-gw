# Деплой с Docker

## 🐳 Docker поддержка

Этот проект включает Docker конфигурацию для легкого деплоя и локальной разработки.

## ⚠️ Важно: PythonAnywhere НЕ поддерживает Docker

PythonAnywhere **не поддерживает** запуск Docker контейнеров на всех планах (бесплатном и платных).

Если вам нужен Docker, рассмотрите альтернативные платформы:
- **Railway** (рекомендуется) - простое развертывание
- **Heroku** - популярная платформа
- **DigitalOcean App Platform** - хорошая производительность
- **Google Cloud Run** - масштабируемость
- **AWS App Runner** - для больших проектов
- **Render** - простая настройка

## 📋 Что включено

### Файлы:
- `Dockerfile` - многостадийная сборка (frontend + backend)
- `docker-compose.yml` - для локального запуска
- `.dockerignore` - исключения для сборки

## 🚀 Локальный запуск с Docker

### Предварительные требования:
- Docker Desktop (Windows/Mac) или Docker Engine (Linux)
- Docker Compose

### Быстрый старт:

1. **Сборка и запуск:**
```bash
docker-compose up --build
```

2. **Запуск в фоне:**
```bash
docker-compose up -d --build
```

3. **Просмотр логов:**
```bash
docker-compose logs -f
```

4. **Остановка:**
```bash
docker-compose down
```

### Доступ к приложению:
- **Frontend + Backend:** http://localhost:8006
- **API Docs:** http://localhost:8006/docs

## 🏗️ Сборка образа

```bash
# Сборка образа
docker build -t gwadm:latest .

# Запуск контейнера
docker run -d \
  -p 8006:8006 \
  -v $(pwd)/backend/santa.db:/app/backend/santa.db \
  -v $(pwd)/backend/uploads:/app/backend/uploads \
  --name gwadm-app \
  gwadm:latest
```

## 📦 Структура Docker образа

### Stage 1: Frontend Builder
- Устанавливает Node.js 18
- Копирует исходники React
- Собирает production build

### Stage 2: Backend + Frontend
- Python 3.10 slim (минимальный размер)
- Устанавливает Python зависимости
- Копирует backend код
- Копирует собранный frontend
- Запускает FastAPI сервер

## 🔧 Настройка для продакшена

### Переменные окружения:

Создайте `.env` файл:

```env
SECRET_KEY=your-secret-key-here
DATABASE_URL=sqlite:///./santa.db
CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
```

Обновите `docker-compose.yml`:

```yaml
services:
  app:
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=${DATABASE_URL}
      - CORS_ORIGINS=${CORS_ORIGINS}
```

## 🌐 Деплой на платформах с Docker

### Railway

1. Создайте аккаунт на [Railway](https://railway.app)
2. Новый проект → Deploy from GitHub repo
3. Выберите репозиторий `gwadm`
4. Railway автоматически обнаружит Dockerfile
5. Настройте переменные окружения
6. Деплой выполнится автоматически

### Render

1. Создайте аккаунт на [Render](https://render.com)
2. New → Web Service
3. Подключите GitHub репозиторий
4. Выберите `gwadm`
5. Настройки:
   - **Build Command:** `docker build -t gwadm . && docker run --rm gwadm`
   - **Start Command:** `docker run -p 8006:8006 gwadm`
6. Deploy

### Heroku

1. Установите Heroku CLI
2. Создайте `heroku.yml`:
```yaml
build:
  docker:
    web: Dockerfile
run:
  web: python backend/main.py
```

3. Деплой:
```bash
heroku login
heroku create gwadm-app
heroku container:push web
heroku container:release web
```

### DigitalOcean App Platform

1. Создайте приложение на [DigitalOcean](https://cloud.digitalocean.com)
2. Connect GitHub → Выберите репозиторий
3. Выберите "Docker" как тип приложения
4. Укажите путь к Dockerfile (./Dockerfile)
5. Настройте переменные окружения
6. Deploy

## 📊 Преимущества Docker

✅ **Изоляция** - все зависимости внутри контейнера
✅ **Консистентность** - одинаковая среда везде
✅ **Масштабируемость** - легко запустить несколько экземпляров
✅ **Портативность** - работает на любой платформе с Docker
✅ **Простота деплоя** - один образ для всего приложения

## 🔍 Проблемы и решения

### Проблема: "Cannot connect to Docker daemon"

**Решение:**
- Убедитесь, что Docker Desktop запущен (Windows/Mac)
- Проверьте права доступа: `sudo usermod -aG docker $USER` (Linux)

### Проблема: "Port already in use"

**Решение:**
- Измените порт в `docker-compose.yml`: `"8007:8006"`
- Или остановите процесс на порту 8006

### Проблема: База данных не сохраняется

**Решение:**
- Проверьте, что volumes правильно смонтированы
- Убедитесь, что пути к базе данных правильные

## 📝 Docker команды

```bash
# Сборка
docker build -t gwadm:latest .

# Запуск
docker run -d -p 8006:8006 --name gwadm gwadm:latest

# Логи
docker logs -f gwadm

# Остановка
docker stop gwadm

# Удаление
docker rm gwadm

# Просмотр запущенных контейнеров
docker ps

# Вход в контейнер
docker exec -it gwadm bash

# Удаление образа
docker rmi gwadm:latest
```

## 🎯 Рекомендации

### Для разработки:
- Используйте `docker-compose` для локального запуска
- Используйте volumes для hot-reload (опционально)

### Для продакшена:
- Используйте multi-stage build (уже настроено)
- Настройте health checks
- Используйте secrets для переменных окружения
- Рассмотрите использование Docker registry (Docker Hub, GitHub Container Registry)

## ✅ Итого

- ✅ Docker конфигурация готова
- ✅ Можно использовать локально
- ✅ Можно деплоить на Railway, Render, Heroku и другие платформы
- ❌ PythonAnywhere НЕ поддерживает Docker

Для PythonAnywhere используйте стандартную инструкцию из `PYTHONANYWHERE_DEPLOYMENT.md`.

