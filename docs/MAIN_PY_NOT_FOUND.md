# Устранение ошибки: main.py exists: False

## 🔴 Проблема

В Server log на PythonAnywhere видно:
```
Project path: /home/gwadm/gwadm/backend
Exists: True
main.py exists: False
```

Это означает, что WSGI файл находит правильный путь к проекту, но не может найти файл `main.py`.

## ✅ Решение

### Шаг 1: Проверьте структуру проекта

В Bash консоли PythonAnywhere выполните:

```bash
cd ~/gwadm
pwd  # Должно быть /home/ВАШ_USERNAME/gwadm
ls -la
```

Ожидаемая структура:
```
gwadm/
├── backend/
│   ├── main.py          ← ДОЛЖЕН СУЩЕСТВОВАТЬ!
│   ├── requirements.txt
│   ├── santa.db
│   └── ...
├── build/
├── src/
├── package.json
└── ...
```

### Шаг 2: Проверьте наличие main.py

```bash
cd ~/gwadm/backend
ls -la main.py
```

Если файл отсутствует — перейдите к Шагу 3.

Если файл существует — проверьте права доступа:
```bash
ls -la main.py
# Должно быть что-то вроде: -rw-r--r-- 1 gwadm gwadm ...
```

### Шаг 3: Обновите проект из GitHub

```bash
cd ~/gwadm
git status  # Проверьте статус
git pull origin master  # Обновите из GitHub
```

### Шаг 4: Проверьте, что файл появился

```bash
cd ~/gwadm/backend
ls -la main.py  # Должен существовать
pwd             # Должно быть /home/ВАШ_USERNAME/gwadm/backend
```

### Шаг 5: Перезапустите приложение

1. Откройте **Web** → **Reload**
2. Проверьте **Server log** — должно быть:
   ```
   main.py exists: True
   ```

## 🔍 Если файл всё ещё не найден

### Вариант A: Неправильная структура проекта

Если вы склонировали проект в неправильное место:

```bash
cd ~
ls -la
# Проверьте, где находится проект
```

Если проект находится в `~/gwadm/gwadm/backend/` (двойной gwadm):

```bash
cd ~/gwadm/gwadm
ls -la backend/main.py  # Проверьте, есть ли там файл

# Если файл там, исправьте WSGI:
# В WSGI файле используйте:
project_path = os.path.join(home_dir, 'gwadm', 'gwadm', 'backend')
```

**НО ЛУЧШЕ:** Переместите проект в правильную структуру:

```bash
cd ~
mv gwadm/gwadm/backend/* gwadm/backend/
# или
rm -rf gwadm/gwadm  # Если это дубликат
```

### Вариант B: Файл не был закоммичен в Git

Проверьте на локальной машине:

```bash
cd ~/Documents/adm-gw
git status
git ls-files backend/main.py  # Должен показывать файл
```

Если файл не отслеживается Git:
```bash
git add backend/main.py
git commit -m "Add main.py"
git push origin master
```

Затем на PythonAnywhere:
```bash
cd ~/gwadm
git pull origin master
```

### Вариант C: Ручное копирование файла

Если Git не работает, можно скопировать файл вручную:

1. На локальной машине скопируйте содержимое `backend/main.py`
2. На PythonAnywhere создайте файл:
   ```bash
   cd ~/gwadm/backend
   nano main.py
   # Вставьте содержимое файла
   ```
3. Сохраните (Ctrl+O, Enter, Ctrl+X)

## ✅ Проверка после исправления

После применения исправлений проверьте Server log:

```
Project path: /home/gwadm/gwadm/backend
Exists: True
main.py exists: True  ← ДОЛЖНО БЫТЬ True!
✅ Successfully imported app
```

Если видите `✅ Successfully imported app` — проблема решена!

## 📚 Связанная документация

- `docs/WSGI_CONFIGURATION.md` — настройка WSGI
- `docs/PYTHONANYWHERE_QUICK_START.md` — быстрый старт
- `docs/TROUBLESHOOTING.md` — общая диагностика

