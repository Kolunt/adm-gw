# Диагностика ошибки 500 на PythonAnywhere

## 🔴 Симптомы

В браузере видно:
```
GET https://gwadm.pythonanywhere.com/ 500 (Internal Server Error)
```

## ✅ Диагностика

### Шаг 1: Проверьте Error log на PythonAnywhere

1. Откройте **Web** → **Error log**
2. Найдите последние ошибки
3. Скопируйте полный текст ошибки

### Шаг 2: Проверьте Server log

1. Откройте **Web** → **Server log**
2. Найдите сообщения при запуске:
   - Должно быть: `✅ WSGI application configured successfully`
   - Если есть ошибки - скопируйте их

### Шаг 3: Частые причины ошибки 500

#### 1. Ошибка в WSGI адаптере

В Error log может быть:
```
WSGI ERROR: Error in ASGI app: ...
```

**Решение:** Проверьте, правильно ли скопирован WSGI файл из `backend/wsgi_pythonanywhere.py`

#### 2. База данных не инициализирована

**Симптом:** Ошибки связанные с таблицами в БД

**Решение:**
```bash
cd ~/gwadm/backend
python3.10 -c "from main import *; from sqlalchemy import inspect; inspector = inspect(engine); print(inspector.get_table_names())"
```

Если таблиц нет, база должна инициализироваться автоматически при первом запуске.

#### 3. Ошибка в FastAPI приложении

**Симптом:** Ошибки в логике приложения при обработке запроса

**Решение:** Проверьте Error log для деталей. Ошибка будет содержать stack trace.

#### 4. Проблемы с зависимостями

**Решение:**
```bash
cd ~/gwadm/backend
pip3.10 install --user -r requirements.txt
```

### Шаг 4: Проверка структуры проекта

```bash
cd ~/gwadm
ls -la backend/main.py  # Должен существовать
ls -la build/index.html  # Фронтенд должен быть собран
```

### Шаг 5: Тестирование вручную

Попробуйте запустить приложение в консоли:

```bash
cd ~/gwadm/backend
python3.10 -c "from main import app; print('App loaded successfully')"
```

Если ошибка - скопируйте её.

## 🔍 Детальная диагностика

### Проверка WSGI конфигурации

Убедитесь, что в WSGI файле используется правильный код из `backend/wsgi_pythonanywhere.py`.

### Проверка импортов

```bash
cd ~/gwadm/backend
python3.10 -c "import sys; sys.path.insert(0, '.'); from main import app; print('Import successful')"
```

### Проверка базы данных

```bash
cd ~/gwadm/backend
ls -la santa.db  # Должна существовать
sqlite3 santa.db "SELECT name FROM sqlite_master WHERE type='table';"
```

## 📝 Что делать с конкретными ошибками

### Ошибка: "Table does not exist"

База не инициализирована. FastAPI должен автоматически создать таблицы при первом запуске, но если этого не произошло:

```bash
cd ~/gwadm/backend
python3.10 -c "from main import Base, engine; Base.metadata.create_all(bind=engine)"
```

### Ошибка: "Import error"

Проверьте зависимости:
```bash
pip3.10 list | grep -E "fastapi|sqlalchemy|pydantic"
```

### Ошибка в WSGI адаптере

Проверьте версию WSGI файла - должен быть последний из репозитория.

## 🆘 Если проблема сохраняется

1. Скопируйте **полный текст** из Error log (последние 100-200 строк)
2. Скопируйте последние строки из Server log (при запуске)
3. Выполните команды диагностики выше и скопируйте результаты

Все это поможет точно определить причину ошибки 500.

