# Быстрое исправление: main.py not found

## 🔴 Проблема

В Server log видно:
```
main.py exists: False
```

## ✅ Решение за 30 секунд

**На PythonAnywhere в Bash консоли:**

```bash
cd ~/gwadm
git pull origin master
ls -la backend/main.py
```

Если файл появился:
1. Откройте **Web** → **Reload**
2. Проверьте **Server log** — должно быть: `main.py exists: True`

Если файл всё ещё отсутствует:

```bash
cd ~/gwadm
git status
git remote -v
git fetch origin
git pull origin master --force
```

Если проблема сохраняется:

```bash
cd ~/gwadm/backend
pwd  # Должно быть /home/ВАШ_USERNAME/gwadm/backend
ls -la  # Покажите список файлов
```

**Возможные причины:**
1. Проект не был обновлен из Git
2. Неправильная структура проекта (проверьте путь в логе - возможно `/home/gwadm/gwadm/backend` означает двойную вложенность)
3. Файл был удален или не был закоммичен

