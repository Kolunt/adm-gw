# Конфигурация GitHub Pages для проекта "Анонимный Дед Мороз"

## 📋 Что такое GitHub Pages?

GitHub Pages — это **статический хостинг**, который автоматически раздает файлы из репозитория. **НЕ требуется настройка сервера** — GitHub сам обрабатывает запросы и отдает HTML/CSS/JS файлы.

## ⚙️ Настройка в GitHub (один раз)

### 1. Включить GitHub Pages в репозитории

1. Откройте https://github.com/Kolunt/gwadm
2. Перейдите в **Settings** → **Pages**
3. В разделе **"Build and deployment"**:
   - **Source**: выберите **"GitHub Actions"**
4. Нажмите **Save**

### 2. Проверить, что GitHub Actions включен

1. В том же репозитории: **Settings** → **Actions** → **General**
2. Убедитесь, что **"Allow all actions and reusable workflows"** включен

## 📁 Файлы конфигурации проекта

### 1. `package.json`
```json
{
  "homepage": "https://kolunt.github.io/gwadm"
}
```
- Указывает базовый URL для сборки React приложения
- React автоматически использует его при сборке (`npm run build`)

### 2. `.github/workflows/deploy.yml`
- GitHub Actions workflow для автоматической сборки и деплоя
- Запускается при каждом push в ветку `master` или `main`
- Собирает React приложение и загружает в GitHub Pages

**Ключевые настройки:**
- `PUBLIC_URL: /gwadm` — базовый путь для всех ресурсов
- `publish_dir: ./build` — папка со собранным приложением
- `node-version: '18'` — версия Node.js для сборки

### 3. `public/404.html`
- Специальный файл для правильной работы SPA (Single Page Application)
- GitHub Pages автоматически использует его для всех 404 ошибок
- Позволяет React Router работать правильно при прямых переходах по URL

### 4. `public/.nojekyll`
- Пустой файл, который отключает обработку Jekyll
- Необходим для корректной работы React приложения

## 🔄 Процесс деплоя

### Автоматический деплой

1. **Push в репозиторий** → GitHub Actions автоматически:
   - Клонирует код
   - Устанавливает зависимости (`npm install`)
   - Собирает приложение (`npm run build`)
   - Загружает в GitHub Pages

2. **Проверка статуса:**
   - Откройте **Actions** в репозитории
   - Найдите workflow **"Deploy to GitHub Pages"**
   - Дождитесь зеленого статуса ✅

### Ручной деплой

1. Откройте **Actions** → **"Deploy to GitHub Pages"**
2. Нажмите **"Run workflow"** → **"Run workflow"**

## 🌐 Результат

После успешного деплоя приложение будет доступно по адресу:
**https://kolunt.github.io/gwadm**

## 📝 Важные моменты

### 1. Роутинг React Router

Приложение использует `BrowserRouter`, который требует поддержки на сервере. Для GitHub Pages:
- ✅ Создан файл `public/404.html` для редиректа
- ✅ Все маршруты будут работать корректно

### 2. Backend API

**Важно:** GitHub Pages раздает только **статический фронтенд** (HTML/CSS/JS).

Backend (FastAPI на порту 8006) должен работать отдельно:
- На локальном сервере разработчика
- На отдельном хостинге (Heroku, Railway, DigitalOcean, и т.д.)
- Или на GitHub Codespaces (для разработки)

### 3. CORS и настройки API

Если backend находится на другом домене, убедитесь:
- Backend разрешает CORS запросы с домена `kolunt.github.io`
- В React коде правильно указан `API_URL` для продакшена

Пример настройки CORS в FastAPI:
```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "https://kolunt.github.io",
        "http://localhost:3000"
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

## 🐛 Решение проблем

### Проблема: 404 при прямом переходе по URL

**Решение:** Убедитесь, что файл `public/404.html` присутствует в репозитории и попал в сборку.

### Проблема: Стили/изображения не загружаются

**Решение:** Проверьте, что `PUBLIC_URL` в workflow и `homepage` в `package.json` совпадают.

### Проблема: Workflow не запускается

**Решение:** 
1. Проверьте, что GitHub Actions включен в Settings
2. Убедитесь, что файл `.github/workflows/deploy.yml` находится в репозитории
3. Проверьте, что вы пушите в ветку `master` или `main`

### Проблема: "There isn't a GitHub Pages site here"

**Решение:**
1. Проверьте Settings → Pages → Source должен быть **"GitHub Actions"**
2. Убедитесь, что workflow успешно выполнился (зеленый статус)
3. Подождите 1-2 минуты после завершения workflow

## 📊 Требования к серверу

GitHub Pages **НЕ требует настройки сервера** — это полностью управляемый сервис.

**Ограничения GitHub Pages:**
- ✅ Бесплатный хостинг статических сайтов
- ✅ Автоматический HTTPS
- ✅ Кастомный домен (опционально)
- ❌ Не поддерживает серверный код (PHP, Python, Node.js серверы)
- ❌ Не поддерживает базы данных
- ❌ Лимит размера репозитория (100 GB) и файлов (100 MB на файл)

## ✅ Итого

**Что уже настроено:**
- ✅ Workflow для автоматического деплоя
- ✅ Файл 404.html для SPA роутинга
- ✅ Файл .nojekyll
- ✅ Правильный PUBLIC_URL в сборке
- ✅ homepage в package.json

**Что нужно сделать в GitHub:**
1. Settings → Pages → Source: **GitHub Actions**
2. Дождаться успешного выполнения workflow
3. Открыть https://kolunt.github.io/gwadm

**Всё! Серверная конфигурация не требуется.**

